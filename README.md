# Minimum Depth of Binary Tree

## Description
This project solves the problem of finding the minimum depth of a binary tree. 
The minimum depth is the number of nodes along the shortest path from the root node to the nearest leaf node.

## Example
```python
Input: root = [3,9,20,null,null,15,7]
Output: 2
python min_depth_tree.py

---

✅ بعد كتابة README، احفظه وادفعه على GitHub بنفس الطريقة:  

```bash
git add README.md
git commit -m "Add README"
git push
git init
git add .
git commit -m "Initial commit - minimum depth of binary tree"
git branch -M main
git remote add origin <paste-your-repo-link-here>
git push -u origin main
